/**
 * Default limits for Railway API queries
 */

/** Default limit for build logs queries */
export const DEFAULT_BUILD_LOGS_LIMIT = 100;

/** Default limit for deployment logs queries */
export const DEFAULT_DEPLOYMENT_LOGS_LIMIT = 100;

/** Default limit for HTTP logs queries */
export const DEFAULT_HTTP_LOGS_LIMIT = 2000;

/** Default limit for HTTP metrics queries (max limit for metric aggregation) */
export const DEFAULT_HTTP_METRICS_LIMIT = 5000;
